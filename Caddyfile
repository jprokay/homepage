:{$PORT}

# Cache static assets aggressively
@static {
    path *.css *.js *.jpg *.jpeg *.png *.gif *.svg *.woff *.woff2 *.ttf *.eot *.webp *.avif
}
header @static Cache-Control "public, max-age=31536000, immutable"

# Cache HTML for shorter time
@html {
    path *.html /
}
header @html Cache-Control "public, max-age=3600, must-revalidate"

# Cache sitemap and other meta files
@meta {
    path /sitemap.xml /llms.txt /robots.txt
}
header @meta Cache-Control "public, max-age=86400"

# Security headers
header {
    X-Content-Type-Options "nosniff"
    X-Frame-Options "SAMEORIGIN"
    Referrer-Policy "strict-origin-when-cross-origin"
    # CSP: allow self, Google Fonts, and inline styles (for critical CSS)
    Content-Security-Policy "default-src 'self'; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; img-src 'self' data:; frame-ancestors 'self'"
    # Permissions Policy
    Permissions-Policy "geolocation=(), microphone=(), camera=()"
}

# Redirect .md URLs to clean URLs (fixes duplicate content)
@postmd path_regexp postmd ^/post/(.+)\.md$
redir @postmd /post/{re.postmd.1} permanent

# Individual post routes
@post path /post/*
handle @post {
    rewrite * /post.html
}

root * /srv
try_files {path}.html
encode zstd gzip
templates
file_server 
